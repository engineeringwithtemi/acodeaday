title: Kth Smallest Element in a BST
sequence_number: 69
difficulty: medium
pattern:
  - tree
  - dfs
  - bst
  - inorder

description: |
  Given the `root` of a binary search tree, and an integer `k`, return the `k`th smallest value (**1-indexed**) of all the values of the nodes in the tree.

  For testing purposes, binary trees are represented as arrays in level-order (BFS), where `null` represents missing nodes.

constraints:
  - "The number of nodes in the tree is n."
  - "1 <= k <= n <= 10^4"
  - "0 <= Node.val <= 10^4"

examples:
  - input: "root = [3,1,4,null,2], k = 1"
    output: "1"
  - input: "root = [5,3,6,2,4,null,null,1], k = 3"
    output: "3"

languages:
  python:
    starter_code: |
      class Solution:
          def kthSmallest(self, root: List, k: int) -> int:
              pass
    reference_solution: |
      class Solution:
          def kthSmallest(self, root: List, k: int) -> int:
              from collections import deque

              class TreeNode:
                  def __init__(self, val=0):
                      self.val = val
                      self.left = None
                      self.right = None

              def build_tree(arr):
                  if not arr or arr[0] is None:
                      return None
                  root = TreeNode(arr[0])
                  queue = deque([root])
                  i = 1
                  while queue and i < len(arr):
                      node = queue.popleft()
                      if i < len(arr) and arr[i] is not None:
                          node.left = TreeNode(arr[i])
                          queue.append(node.left)
                      i += 1
                      if i < len(arr) and arr[i] is not None:
                          node.right = TreeNode(arr[i])
                          queue.append(node.right)
                      i += 1
                  return root

              def inorder(node, result):
                  if node:
                      inorder(node.left, result)
                      result.append(node.val)
                      inorder(node.right, result)

              tree = build_tree(root)
              values = []
              inorder(tree, values)
              return values[k - 1]
    function_signature:
      name: kthSmallest
      params:
        - name: root
          type: List
        - name: k
          type: int
      return_type: int

test_cases:
  - input: [[3, 1, 4, null, 2], 1]
    expected: 1
  - input: [[5, 3, 6, 2, 4, null, null, 1], 3]
    expected: 3
  - input: [[1], 1]
    expected: 1
  - input: [[2, 1], 1]
    expected: 1
  - input: [[2, 1], 2]
    expected: 2
  - input: [[2, 1, 3], 2]
    expected: 2
  - input: [[5, 3, 7, 2, 4, 6, 8], 1]
    expected: 2
  - input: [[5, 3, 7, 2, 4, 6, 8], 7]
    expected: 8
  - input: [[5, 3, 7, 2, 4, 6, 8], 4]
    expected: 5
  - input: [[3, 1, 4, null, 2], 2]
    expected: 2
  - input: [[3, 1, 4, null, 2], 3]
    expected: 3
  - input: [[41, 37, 44, 24, 39, 42, 48, 1, 35, 38, 40, null, 43, 46, 49, 0, 2, 30, 36, null, null, null, null, null, null, 45, 47], 10]
    expected: 39
  - input: [[50, 25, 75, 10, 30, 60, 90], 5]
    expected: 60
  - input: [[10, 5, 15, 3, 7, 12, 20], 3]
    expected: 7
  - input: [[100, 50, 150], 1]
    expected: 50
